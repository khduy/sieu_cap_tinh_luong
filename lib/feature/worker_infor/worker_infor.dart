import 'package:flutter/material.dart';

class WorkerInfor extends StatelessWidget {
  const WorkerInfor({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}